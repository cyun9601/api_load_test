[project]
name = "load-test"
version = "0.1.0"
description = "STT 모델 로드 테스트 도구"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.9.0",
    "openai>=1.0.0",
    "numpy>=1.24.0",
    "pyyaml>=6.0.0",
    "matplotlib>=3.7.0",
]
